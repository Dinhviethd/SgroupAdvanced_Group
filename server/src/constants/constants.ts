export enum TypeVisibility {
  WORKSPACE = 'workspace',
  PRIVATE = 'private',
  PUBLIC = 'public',
}

export enum TierWorkspace {
  FREE = 'free',
  STANDARD = 'standard',
  PREMIUM = 'premium',
  ENTERPRISE = 'enterpise',
}

export enum Visibility {
  PRIVATE = 'private',
  PUBLIC = 'public',
}
export enum RoleScope {
  WORKSPACE = 'workspace',
  BOARD = 'board',
}